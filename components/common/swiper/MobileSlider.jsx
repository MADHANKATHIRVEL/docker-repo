import "./mobile-swiper.scss";

const MobileSlider = ({ children }) => {
  return (
    <section className="mobile-swiper-container">
        {children}
    </section>
  );
};

export default MobileSlider;
